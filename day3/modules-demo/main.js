import * as math from "./math";

function doSomething() {
	console.log("addition result is" + math.sum(10, 20));
	console.log("division result is" + math.divide(10, 5));
}

export function startApp() {}
